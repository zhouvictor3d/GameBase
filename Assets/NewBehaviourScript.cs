using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class NewBehaviourScript : MonoBehaviour
{


  
    // Use this for initialization
    void Start()
    {
        string dd = ReplaceRichSignal(mm);
        Debug.Log("dd===>" + dd);
    }

    // Update is called once per frame
    void Update()
    {

    }
}
